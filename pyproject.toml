[project]
name = "track"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "hrjeon", email = "hrjeon11@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "pygame>=2.6.1",
]

[project.scripts]
track = "track:main"

[build-system]
requires = ["uv_build>=0.12.1,<0.13.0"]
build-backend = "uv_build"
